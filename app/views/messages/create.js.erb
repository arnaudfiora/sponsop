<% if @message.errors.any? %>
  // handle validation errors later
  console.log("error")
<% else %>
  var messagesContainer = document.querySelector('#chat_room-<%= @message.chat_room.id %>').querySelector('.messages');
  messagesContainer.insertAdjacentHTML('beforeend', '<%= j render "message", message: @message, user_is_messages_author: true %>');
  console.log(document.querySelectorAll('.message-input'))
 document.querySelectorAll('textarea').forEach( (input) =>  input.value = "" )
<% end %>
